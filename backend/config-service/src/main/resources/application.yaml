server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/Duvan-Vergara/Tingeso_Lab_3
          search-paths: config-data
  application:
    name: config-service